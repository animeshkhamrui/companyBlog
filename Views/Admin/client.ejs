<%- include('Common/header') %>
<%- include('Common/sidebar') %>
<!-- Page Content  -->
<div id="content" class="p-4 p-md-5 pt-5">
  <% if(message.length>0) { %>
    <div class="alert alert-success text-center">
        <%= message %>
    </div>
    <% } %>
    <h2 class="mb-4">All Client</h2>
    <div><a href="/admin/add-client/page"><button class="btn btn-large btn-success">Add Client</button></a></div>
    <div>
        <table class="table">
            <thead>
              <tr>
                <th scope="col" class="text-center">#</th>
                <th scope="col" class="text-center">Image</th>
                <th colspan="2" class="text-center">Action</th>
              </tr>
            </thead>
           <% if(allclient.length>0) { %>
            <% let i=0 %>
            <tbody>
                <% for(let x of allclient) { %>
                    <% i+=1 %>
              <tr>
                <th  class="text-center" scope="row"><%= i %></th>
                <td  class="text-center" ><%= x.name %></td>
                <td  class="text-center"><img src="./../../Upload/ClientImage/<%= x.image %>" height="50px" width="50px"/></td>
                <td  class="text-center"><a href="/admin/edit-client/page/<%= x._id %>"><button class="btn btn-success">Edit</button></a></td>
                <td  class="text-center"><a href="/admin/delete-client/<%= x._id %>"><button class="btn btn-danger">Delete</button></a></td>
              </tr>
              <% } %>
            </tbody>
            <% }else{ %>
                <div>No Client Available...</div>
                <% } %>
          </table>
    </div>
    </div>
<%- include('Common/footer') %>